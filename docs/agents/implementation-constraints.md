# Agent 实现约束

## 插件通用性

所有插件功能的实现必须具备通用性，不得针对单一网页进行硬编码。目标是实现跨网页复用能力，而不是为某个页面编写定制脚本。

### 强制要求

1. 不得针对特定 URL、域名或单一页面结构编写专用逻辑。
2. 必须基于通用 DOM 规则、语义选择器或可配置策略实现功能。
3. 如确需特殊处理，必须使用可扩展的规则系统，不得增加写死的条件分支。
4. 页面适配逻辑必须模块化，并与采集、转换等核心逻辑解耦。
5. 所有修改都必须保证可用于多个结构不同的网页，并通过多页面场景验证。

### 实现与评审标准

- 不得以 URL、域名、站点名称或固定 DOM 层级作为功能判断依据。
- 优先使用 HTML 语义、ARIA、元素角色、计算样式和可配置规则识别页面内容。
- 页面差异应封装在独立适配模块或规则配置中，核心流程不得感知具体站点。
- 新增规则应具有明确的匹配范围、优先级和回退行为，且能够独立扩展或移除。
- 测试应覆盖多个域名及不同 DOM、布局和组件结构；只在单一目标页面通过，不视为完成。
