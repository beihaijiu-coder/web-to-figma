# Web to Figma

将用户浏览器中已经渲染的网页转换为可在 Figma 中继续编辑的设计结构；本词汇表统一产品范围与转换结果的表达。

## Language

**全网页转换**:
对用户发起的任意网页转换请求都进入转换流程，不以网页归属、登录状态、内容类型或页面类型设置产品层面的转换门槛；受浏览器或 Figma 能力限制的内容走降级处理。
_Avoid_: 白名单网页、仅支持公开网页、仅支持授权网页

**降级**:
当网页内容无法可靠地转换为原生可编辑 Figma 节点时，保留其当前视觉结果并在转换报告中说明限制的处理结果。
_Avoid_: 不支持、导入失败（仅指单个内容）

**混合转换**:
以页面整体视觉正确为底线，将常规文字、容器、按钮和图片转换为原生可编辑节点，并将无法可靠转换的复杂局部降级为栅格结果；不得将整页降级为单张图片。
_Avoid_: 纯截图转换、强制全量可编辑

## Commercial Model

**Pro 无限转换**:
专业版不按单次转换或点数收费，正常使用下可无限发起转换；仅受为可靠性和防滥用而设的单任务技术上限约束。
_Avoid_: 转换点数、按次扣费

**云端中转**:
用于在浏览器扩展与 Figma 插件之间短暂加密传递转换任务的服务，不承担网页转换的主要计算。
_Avoid_: 云端转换引擎、长期任务仓库

**个人版产品**:
面向全球个人用户销售的产品范围，仅提供 Free 与 Pro 两个版本；团队席位、企业版和私有化部署不在当前产品范围内。
_Avoid_: 团队版、企业版

**免费周额度**:
Free 用户每周可完成 2 次完整页面或组件转换；额度按自然产品周重置。
_Avoid_: 一次性试用额度、免费无限转换

**Pro 订阅周期**:
Pro 同时提供月付与年付，年付以明显优惠作为默认推荐方案；两个周期的功能权益一致。
_Avoid_: 仅年付、仅月付、按次购买

**Pro 首发定价**:
Pro 的首发价格为 $12/月或 $96/年；两个周期均提供无限转换，且仍受单任务技术上限约束。
_Avoid_: 转换点数、功能分裂的月付与年付

**单页转换**:
首发版本中，一个转换任务只处理用户当前选择的一个网页；该网页按当前浏览器实际渲染的单一视口状态导入。
_Avoid_: 批量页面导入、自动生成多断点

**采集方式**:
首发仅提供完整页面采集与用户选择的单个组件采集两种方式。
_Avoid_: 当前视口采集、批量页面采集

**Figma 单平台**:
首发版本唯一支持的设计目标是 Figma；其他设计工具不属于当前对用户承诺的导出目标。
_Avoid_: 多设计平台支持、跨工具导出

**Chrome 首发支持**:
首发版本唯一正式支持的网页采集浏览器是桌面版 Chrome；其他浏览器不在首发测试和客服支持范围内。
_Avoid_: 全浏览器支持、移动浏览器支持

**Figma URL 发起**:
用户可在 Figma 插件中输入 URL 发起转换，但网页采集由已安装的 Chrome 扩展执行，再将短时任务回传 Figma；Figma 插件不直接作为网页抓取器。
_Avoid_: Figma 直接抓取 URL、云端爬虫转换

**原始高清图片**:
Pro 尽可能保留源站原始分辨率的图片资产；Free 使用适配当前显示尺寸的图片。两种图片均在 Figma 中作为可替换的图片节点。
_Avoid_: Free 不可编辑图片、全量无差别原图

**确定性图层命名**:
首发根据网页语义、ARIA、元素角色和文本生成稳定且可读的 Figma 图层名称，不使用生成式 AI 重命名。
_Avoid_: AI 图层命名、随机命名

**导入布局偏好**:
用户在每次导入前选择“视觉还原优先”或“可编辑布局优先”；前者偏向固定坐标，后者尽可能转换为 Figma 自动布局。
_Avoid_: 系统强制单一布局策略

**静默字体替换**:
网页字体在 Figma 中不可用时，自动匹配最接近的可用字体并继续导入；用户可在导入前指定统一替代字体，不以字体缺失中断任务。
_Avoid_: 缺失字体阻断导入、字体替换报告

**极简完成反馈**:
成功导入后直接定位生成的 Figma 结果，不向用户展示转换报告、节点统计或常规警告；详细诊断仅用于内部排障。
_Avoid_: 转换报告页、技术指标面板

**失败反馈**:
只有整次导入无法完成时，向用户显示简短原因和重试入口；局部内容已可视降级时不额外打扰用户。
_Avoid_: 技术错误码、局部降级警告轰炸、静默吞掉整次失败

**取消清理**:
用户取消导入时立即停止当前任务，并删除该任务已经创建的全部半成品 Figma 图层，然后回到开始状态。
_Avoid_: 残缺画布、保留半成品、后台继续导入
